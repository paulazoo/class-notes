Transformation $T$: transfers $x$ in domain $R^m$ to $T(x)$ codomain $R^n$

Linear transformation: every term must be a constant times variable (e.g. $2x$)

Linear transformations allow for multiplication and addition